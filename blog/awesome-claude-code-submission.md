# Submission draft → hesreallyhim/awesome-claude-code (45.8k ⭐)

**Do NOT submit yet.** That list has an anti-spam policy with an **age gate**
("the resource has been public for over one ___") and *harsh penalties* for
non-compliant/self-promo submissions. Submit only when **all** are true:

- [ ] Repo is past the age gate (created 2026-06-06 → safe after ~2026-06-20; re-read the exact checkbox first).
- [ ] `npx github:nandukmelath/engram` works **or** the plugin install is verified.
- [ ] A few real stars (not 0) and at least one external user/issue.
- [ ] All README links work.

**Submit via the form** (don't paste a raw issue):
https://github.com/hesreallyhim/awesome-claude-code/issues/new?template=recommend-resource.yml

Fill the fields with:

| Field | Value |
| --- | --- |
| **Display Name** | Engram |
| **Category** | Tooling *(pick "Hooks" if offered — it fits too)* |
| **Sub-Category** | Memory / Knowledge |
| **Primary Link** | https://github.com/nandukmelath/engram |
| **Author Name** | Nandu |
| **Author Link** | https://github.com/nandukmelath |
| **License** | MIT |

**Description:**

> Persistent memory for Claude Code. Two hooks archive every session to an
> Obsidian vault (plain Markdown) and inject the last few days of work into each
> new session; a `/recall` command searches your whole history with citations.
> Zero runtime dependencies, secrets redacted before write, fully local.

**Validate Claims / security notes** (this list cares — state plainly):

> - No network calls (fully local; never contacts any server).
> - No telemetry.
> - Does **not** require bypass-permissions mode.
> - Hooks are short, commented Node scripts that silent-fail (never block a session).
> - MIT licensed.

**Additional comments:** mention it ships as both a Claude Code plugin and a
zero-dep npm CLI, and is cross-platform (Linux/macOS/Windows).

> Tip: submit a day or two **after** the Show HN / Reddit launch, once there's
> traction — curated lists are far more receptive to a resource people already use.
